import React from 'react';

export default (props) => {
    return (
        <footer>
            <a href="#">All</a>
            <a href="#/active">Active</a>
            <a href="#completed">Completed</a>
        </footer>
    )
}